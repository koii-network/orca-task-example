# Server Configuration

# Middle Server configuration
HOST=localhost
PORT=5001
MIDDLE_SERVER_HOST=localhost
MIDDLE_SERVER_PORT=5001

# Task Server configuration (used by namespace-wrapper internally)
TASK_SERVER_PORT=3000

# Python Server configuration
PYTHON_SERVER_HOST=localhost
PYTHON_SERVER_PORT=8080
PYTHON_COMMAND=python3

# Wallet Configuration
# Path to your Solana wallet JSON file (absolute path recommended)
STAKING_WALLET_PATH="/path/to/your/wallet.json"

# IPFS Configuration (optional)
# IPFS_API_KEY=your_ipfs_api_key

# Orca Configuration
# If you need to customize the Python environment
# PYTHON_PATH=/usr/bin/python3
